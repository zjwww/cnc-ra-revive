# 更新日志

[项目说明](README.zh-CN.md) · [English changelog](CHANGELOG.md) · [English README](README.md) · [在线查看](https://zjwww.github.io/cnc-ra-revive/)

这里的版本号用于标记信息图及其查看页面，不代表图中游戏或社区引擎的发布版本。本地继续按版本分别归档；从 v1.2 起，GitHub 当前文件列表仅保留最新文件，旧版本仍可通过 Git 历史查看。

## v1.11 — 2026-09-12

- 四个文件的浏览器标签页标题统一使用 CnC-RA-Revive 前缀与 Native Scale / Viewer Scale 英文名称，手机标题同步统一。
- 恢复独立 SVG 文档标题及桌面 Viewer Scale 百分比更新；HTML 标题继续放在内嵌 SVG 之外。项目链接地址提示保持不变。
- 保留图像、等比尺寸计算与缩放操作。

- 验证：四个文件的桌面及移动模拟标题、Viewer 110% 与重置、相较 v1.10 完全一致的截图及文字位置、11 处原链接、非整数 DPI 宽度适配与模拟双指缩放均通过。此前归档版本保持原样。未进行 iPhone Safari 真机测试。

## v1.10 — 2026-09-11

- 修复 v1.8 起桌面初始适配尺寸出现多余横向滚动条的问题：`clientWidth` 的整数取整可能超过实际可用的小数宽度，已在 150% 显示缩放下复现。
- 改用精确可用视口宽度并向下对齐物理像素，高度按比例计算。主动放大后的横向滚动继续保留；手机手势处理、v1.9 超链接和提示行为保持不变。
- 经用户实测确认后发布，包含 v1.9 的 11 处项目链接及提示调整。原生 HTML 继续作为 Pages 首页，此前所有本地版本完整保留。

- 验证：在 150% 显示缩放下复现旧版小数溢出；五档显示缩放、四个文件共记录 165 项检查，包含适配时横向滚动范围为零、横向滚动条不占高度、主动放大和恢复。四个移动端模拟文档、44 处链接及 48 次新标签页打开检查通过；100% 下默认像素和文字几何与 v1.9 一致。本次未新增 iPhone Safari 真机测试。

## v1.9 — 本地候选版，改动随 v1.10 发布

- 两组 HTML/SVG 同步加入 11 处项目文字链接：右侧标题、对比表头、EA 四个仓库及 Modding Support，使用指定地址，支持悬停／聚焦下划线、地址提示和新标签页打开请求。
- 取消整张 SVG 的标题悬停提示。HTML 保留动态标签页标题，独立 SVG 显示文件名，避免缩放后重新出现提示。
- 保留图像几何布局、宽度适配、桌面缩放操作和手机原生手势。最初作为本地候选版，经用户确认后随 v1.10 一起发布。

- 验证：四个文件的默认画面像素及 177 个文字边界与 v1.8 一致；44 处链接、48 次鼠标／键盘新标签页打开、下划线及根标题提示取消检查通过；96 项桌面缩放和四个移动端模拟文档检查通过。跳转测试使用替代目标页面核对地址，本次不是 iPhone Safari 真机测试。

## v1.8 — 2026-09-10

- 四个 HTML/SVG 查看文件统一改为按可用宽度适配，高度保持 16:9，超出窗口时纵向滚动，并计入桌面滚动条占位。
- 发布经用户试用确认的 v1.7 手机手势处理：浏览器双指缩放和平移、旋转稳定后适配、手机端不主动调整滚动。桌面继续保留两套缩放机制。
- 保留原图、177 个文字元素及 v1.6 箭头修复，原生 HTML 继续作为 Pages 首页。

- 验证：24 项宽度适配检查覆盖 4K、窄窗口和滚动条临界尺寸；96 项桌面缩放检查覆盖两种显示缩放。四个文件的移动端模拟及手势／旋转状态检查通过。除共用脚本和版本号外，图像内容与 v1.7 字节一致。本次未新增 Safari 真机验证。

## v1.7 — 本地候选版，改动随 v1.8 发布

- 两组 HTML/SVG 在手机上统一使用浏览器双指缩放和平移，不主动调整滚动位置，也不套用桌面 DPR 补偿。桌面保留原有两套缩放模式。
- 增加稳定视口测量、被动触摸检测和旋转稳定检查：150 毫秒静默期、连续两帧、1 像素容差及 800 毫秒放弃上限。工具栏引起的单纯高度变化不重新适配。
- 保留完整原图和 v1.6 箭头修复，最初作为本地候选版，随后经用户试用确认，移动端改动随 v1.8 发布。
- 验证：实际脚本通过受控事件／计时检查，涵盖触摸期间暂停适配、工具栏变化、旋转稳定与超时行为。四个 Edge 移动端模拟文档在模拟双指缩放和高度变化时保持布局尺寸，旋转后重新适配，全程无脚本滚动调用。桌面在 100% 和 150% 显示缩放下通过 96 个回归检查点。这些检查不等同于 iOS Safari 真机验证。

## v1.6 — 2026-09-10

- 修复绿色 OpenTS 源码关系、红色源码重建关系、黄色 Red Alert 2 兼容关系三处实线箭头的裁切问题。
- 增加三个箭头专用发光滤镜，使用明确坐标范围和 48 个坐标单位的余量，容纳箭头与光晕。面板滤镜、箭头坐标、颜色、线宽、三角形形状、图层顺序、全部文字及缩放代码保持不变。
- 经用户实测确认后，同步发布原生 HTML/SVG 和页面内 HTML/SVG。所有本地旧版本保留，Pages 继续以原生 HTML 作为首页。
- 在 Edge 中检查四个文件共 12 个箭头局部，确认中心线下方此前缺失的像素已恢复。四个文件均通过 3840 × 2160、1920 × 1080、1280 × 800 窗口下的等比缩放、16:9 比例、原点位置及 177 个文字元素检查，页面内 150% 缩放也通过。文档小缩略图已刷新。

## v1.5 — 2026-09-10

- 新增原生 HTML/SVG 与页面内缩放 HTML/SVG，共四个可单独使用的完整文件。
- 同一套的两个格式使用相同尺寸与交互代码，保留固定的 3840 × 2160 viewBox，并用同一个比例计算宽、高。原有图片、坐标及 177 个文字元素全部保留。
- 原生组把缩放操作交给浏览器，通过会话存储让刷新保留首次适配基准。初始浏览器倍率决定此基准，不假定它一定是 100%。
- 页面内组支持 Ctrl + 滚轮、加减键及零键，范围 25%～500%，保留鼠标锚点及窗口适配，并在标签页标题显示倍率。浏览器菜单倍率仍是独立状态。
- 经用户实测确认后发布。原生 HTML 作为 Pages 首页（`index.html`），原生 SVG 保留原有 `RA-4K.svg` 地址。另一套使用 `CnC-RA-Revive-4k-Viewer.html` 和 `CnC-RA-Revive-4k-Viewer.svg`。
- 本地 v1.0～v1.5 存档全部保留。GitHub 根目录仅包含最新四个查看文件、缩略图、文档和配置，不创建 GitHub Release。
- 初始适配、脚本限制及跨显示器验证边界见 README。

### 验证

- Microsoft Edge：在 100% 和 150% 显示缩放下，对四个文件记录 96 个检查点。验证原生 80%、100%、110%、150%、200% 的实际画面尺寸、刷新保留、窗口调整，以及滚动到右侧和底部。
- 触发页面内键盘及 Ctrl + 滚轮事件，检查 25%／500% 边界、重置、标题倍率和浏览器 DPR 保持不变。原生组检查未取消快捷键默认行为；实际原生渲染通过 Edge 缩放设置验证，未模拟物理键盘操作。
- 在首次 150% 打开及刷新时，本地文件和临时 localhost HTTP 服务中的 HTML、SVG 均符合已说明的初始适配行为；同时验证两个 HTTP 页面内查看器。未使用公网部署。

## v1.4 — 本地候选版，尚未发布

### 修复

- 将独立 SVG 外层属性与 CSS 中的自动高度改为 `100%`。浏览器 100% 缩放时按窗口宽、高容纳完整图像，原生缩放现在会实际放大或缩小画面及文字。
- 保留左上角对齐与等比缩放。全部 177 个文字元素、图片及内部坐标保持不变，SVG 未增加脚本或快捷键拦截。
- HTML 和缩略图与 v1.2 字节一致；此前所有本地版本及已发布仓库的工作副本均保留原样。

### 验证与发布状态

- 复现 v1.3 的放大失效：浏览器 150% 时，画面及文字实际显示尺寸仍约为原来的 100%。上一轮验证只检查了对齐和横纵比例，遗漏了实际放大效果。
- 在隔离测试配置的 Edge 中，对同一个已打开的文档连续设置原生缩放：110%、125%、150%、200%、80%、50%，再回到 100%。检查画面与文字的实际尺寸是否匹配倍率、原点是否偏移、横纵缩放比例是否一致。
- 验证 200% 时可滚动至右侧和底部、调整窗口大小后的适配，以及 100% 和 150% 显示缩放环境。自动化使用 Edge 原生缩放设置；实际鼠标滚轮和键盘操作仍待用户确认。
- 仅保存本地候选版，未提交、推送或部署 GitHub Pages，等待用户实测确认及明确发布授权。

## v1.3 — 本地候选版，尚未发布；已由 v1.4 接替

已知限制：本候选版消除了左侧留白，但未解决超过 100% 后无法实际放大的问题。相关修复已纳入 v1.5。

### 修复

- 仅将独立 SVG 外层对齐方式由 `xMidYMin meet` 改为 `xMinYMin meet`，消除浏览器原生缩放超过 100% 时逐渐增大的左侧留白。
- 保留等比缩放、全部 177 个文字元素、插画及内部几何结构。HTML 和缩略图均为 v1.2 的未修改副本。

### 验证与发布状态

- 使用隔离的 Microsoft Edge 测试配置，通过真实的浏览器页面缩放设置复现旧版左侧留白。
- 在 80%、100%、110%、125%、150%、200% 六档对比 v1.2 与 v1.3；候选版左侧原点偏移均为 0，横纵缩放比例一致。
- 仅生成本地候选版，未执行 Git 提交、推送或 Pages 部署。等待用户实际测试并明确批准后发布。

## v1.2 — 2026-09-10

### 调整

- HTML 新增整图缩放：支持 Ctrl + 滚轮、Ctrl + 加减键（含数字小键盘），以及 Ctrl + 0 恢复适配窗口。
- 在 25%～500% 缩放范围内保持布局比例；滚轮缩放以鼠标位置为中心，放大后可通过滚动视口查看内容。
- 调整窗口大小时保留相对缩放倍数，刷新页面恢复适配。上述操作调整页面内查看器，不改变浏览器菜单中的缩放百分比。
- 保留独立 SVG 行为，以及全部 177 个文字元素和插画几何结构。
- GitHub 发布目录改为根目录的最新 `index.html` 和 `RA-4K.svg`；从当前文件列表移除版本目录与完整 PNG，不重写历史、不删除本地归档。
- README 新增 960 × 540、约 106 KiB 的 WebP 缩略图。
- 英文 README 的语言切换链接改为“简体中文”，同步更新两份 README、两份日志和历史版本链接。

### 验证

- 在 Microsoft Edge 中检查键盘和鼠标缩放、重置、鼠标位置保持、滚动、窗口调整、缩放边界及五种适配窗口尺寸。
- 核验本地原有 v1.0／v1.1 文件与独立 SVG 未被修改。

## [v1.1](https://github.com/zjwww/cnc-ra-revive/blob/046f4957a5c2baacc75b7e6c28c625633117c319/v1.1/RA-4K.html) — 2026-09-10

### 调整

- 2026-09-07：HTML 改为自适应布局，整张图随浏览器宽度缩放，保持 16:9 比例和模块位置。
- 2026-09-10：新增可独立打开的自适应 SVG，保留原有 3840 × 2160 viewBox、文字、插画和图表几何结构。
- 窗口高度不足时保留纵向滚动。

### 发布整理

- 以 `cnc-ra-revive` 为名整理首次 GitHub 发布，网站首页使用 v1.1。
- 新增配套英文、简体中文 README 和更新日志，并提供顶部导航链接。
- 使用 GitHub Pages 和 `.nojekyll`，从 `main` 分支直接发布静态文件。
- 收录最初的 v1.0 HTML、SVG 和 PNG 归档，文件内容保持不变。
- 发布时保留 v1.1 HTML 和 SVG 原始内容；`index.html` 与版本目录中的 HTML 完全一致。
- 未创建 GitHub Release，通过 Git 提交和保留的版本目录记录发布内容。

### 验证

- 在 Microsoft Edge 中验证 HTML 和 SVG，窗口尺寸包括 3840 × 2160、1920 × 1080、1280 × 800、1000 × 450 和 390 × 844。
- 确认横纵缩放比例一致、无横向溢出，全部 177 个文字元素得到保留。
- 发布前核验归档文件哈希值。

## [v1.0](https://github.com/zjwww/cnc-ra-revive/blob/046f4957a5c2baacc75b7e6c28c625633117c319/v1.0/RA-4K.html) — 2026-09-06

### 新增

- 最初的 3840 × 2160 信息图，提供 PNG、可编辑 SVG 和固定尺寸 HTML。
- 保留左侧官方游戏、中央关系箭头、右侧社区项目，以及底部对比表和图例的布局。
- 在生成的科幻插画上重新绘制文字、边框、箭头和表格结构。

### 已知限制

- 画布和 PNG 导出尺寸为 4K，但装饰位图素材并非原生 4K。
- 原始固定尺寸 HTML 不会自动适配较窄的浏览器窗口。
- 源码状态和兼容性说明属于整理时的内容快照，范围说明和上游参考见 README。
